//const { listAllManifests } = require('../../manifest/intf/utils');

module.exports = async(adtClient) => {
    /*const allIntfManifests = await listAllManifests(adtClient);
    const obj = allIntfManifests.find(o => o.manifest.name === 'trm');
    if(obj){
        return obj.manifest.version;
    }
    circular dependency fixme
    */
    return 'FIXME'
}